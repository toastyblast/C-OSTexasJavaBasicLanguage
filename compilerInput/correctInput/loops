

<COM> "This for loop declares the new variable D" </COM>
For (B, 5, B > 3, B += -1)
    Disp B
End
<COM> "As side note D will only be available within the for loop as it was declared within said scope" </COM>
